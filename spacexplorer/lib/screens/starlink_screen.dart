import 'package:flutter/material.dart';

class StarlinkScreen extends StatelessWidget {
  static const String routeName = '/starlink';
  const StarlinkScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Starlink"),
    );
  }
}
